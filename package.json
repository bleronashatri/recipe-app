{
  "name": "recipe-app",
  "version": "1.0.0",
  "description": "Recipe management REST API with frontend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:unit": "vitest run",
    "test:api": "newman run tests/api/collection.json --env-var baseUrl=http://localhost:3000",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "newman": "^6.1.3",
    "vitest": "^1.6.0",
    "wait-on": "^7.2.0"
  }
}
